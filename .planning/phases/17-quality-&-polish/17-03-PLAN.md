---
phase: 17-quality-and-polish
plan: 03
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - frontend/README.md
autonomous: true

must_haves:
  truths:
    - "Developer can read README and set up frontend in under 5 minutes"
    - "README documents all available npm scripts including type-check"
    - "README explains project structure with directory descriptions"
    - "README lists prerequisites (Node.js, npm, backend API)"
    - "README describes key patterns (React Query hooks, form validation, responsive design)"
  artifacts:
    - path: "frontend/README.md"
      provides: "Comprehensive developer documentation for frontend setup and patterns"
      min_lines: 80
  key_links:
    - from: "frontend/README.md"
      to: "frontend/package.json"
      via: "Documents npm scripts"
      pattern: "npm run dev|npm run build|npm run lint|npm run type-check"
---

<objective>
Replace the default Vite template README with comprehensive developer documentation for the frontend application.

Purpose: Satisfy QUAL-UI-05 (README for 5-minute setup). The current README.md is the default Vite scaffolding template with no project-specific information. Replace it with a complete guide covering prerequisites, quick start, npm scripts, project structure, key patterns, and troubleshooting.
Output: A README.md that enables any developer to clone, install, and run the frontend in under 5 minutes.
</objective>

<execution_context>
@/Users/craigcronin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/craigcronin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-quality-&-polish/17-RESEARCH.md
@frontend/package.json
@frontend/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write comprehensive frontend README</name>
  <files>frontend/README.md</files>
  <action>
Replace the entire contents of `frontend/README.md` with a comprehensive developer guide. Use the structure outlined in the 17-RESEARCH.md README section as a starting point, but customize with actual verified details from the codebase.

**Required sections:**

1. **Title and description**: "MTI WiFi Monitor - Frontend" with one-line description: React + TypeScript application for managing factory and gateway configurations.

2. **Prerequisites**:
   - Node.js 20+ (how to check: `node --version`)
   - npm 10+ (how to check: `npm --version`)
   - Backend API running at http://localhost:3000 (Vite proxy forwards /api requests)
   - PostgreSQL database (required by backend)

3. **Quick Start** (numbered steps, target < 5 minutes):
   1. Navigate: `cd frontend`
   2. Install: `npm install`
   3. Start dev server: `npm run dev`
   4. Open http://localhost:5173
   Note: The Vite dev server proxies `/api` requests to `http://localhost:3000`. No `.env` configuration needed for local development.

4. **Available Scripts** (table format):
   | Command | Description |
   |---------|-------------|
   | `npm run dev` | Start development server with HMR at http://localhost:5173 |
   | `npm run build` | TypeScript check + production build (outputs to `dist/`) |
   | `npm run preview` | Preview production build locally |
   | `npm run lint` | Run ESLint on all TypeScript files |
   | `npm run type-check` | Run TypeScript compiler check (no emit) |

5. **Project Structure** (tree view with descriptions):
   ```
   src/
   +-- components/         # UI components
   |   +-- forms/         # Form components with Zod validation
   |   +-- layout/        # App shell (AppLayout, Sidebar)
   |   +-- ui/            # shadcn/ui primitives (Button, Input, Dialog, etc.)
   +-- hooks/             # React Query hooks for API integration
   +-- lib/               # Utilities (API client, cn helper, query client)
   +-- pages/             # Route page components
   +-- types/             # TypeScript type definitions (mirrors backend schemas)
   +-- main.tsx           # App entry with routing, React Query, and toast setup
   +-- index.css          # Tailwind CSS imports and theme variables
   ```

6. **Key Patterns** (brief descriptions, not full code):
   - **Data Fetching**: React Query hooks in `src/hooks/` (e.g., `useFactories()`, `useGateways()`). 5-minute stale time, single retry, no refetch on window focus.
   - **Forms**: React Hook Form + Zod validation in `src/components/forms/`. Each form exports its Zod schema and inferred TypeScript type. Error messages display below each field.
   - **State Management**: React Query for all server state. React useState only for UI state (dialog open/close, selected items).
   - **Notifications**: Sonner toast for success/error feedback on all CRUD operations.
   - **Styling**: Tailwind CSS v4 with mobile-first responsive design. CSS variables defined in `index.css`. shadcn/ui components for consistent design.
   - **Routing**: React Router v7 with createBrowserRouter. AppLayout wraps all routes. Root `/` redirects to `/factories`.
   - **API Client**: Custom fetch wrapper in `src/lib/api.ts`. Handles JSON parsing, error extraction, 204 No Content responses.

7. **Tech Stack** (table format):
   | Technology | Version | Purpose |
   |-----------|---------|---------|
   | React | 19.2 | UI framework |
   | TypeScript | 5.9 | Type safety (strict mode) |
   | Vite | 7.2 | Build tool and dev server |
   | Tailwind CSS | 4.1 | Utility-first CSS |
   | React Query | 5.90 | Server state management |
   | React Hook Form | 7.71 | Form handling |
   | Zod | 4.3 | Schema validation |
   | shadcn/ui | - | Component primitives |
   | React Router | 7.13 | Client-side routing |
   | Sonner | 2.0 | Toast notifications |

8. **Troubleshooting**:
   - **Port 5173 in use**: `npm run dev -- --port 5174`
   - **API connection errors**: Verify backend running at http://localhost:3000. Check Vite proxy config in `vite.config.ts`.
   - **TypeScript errors**: Run `npm run type-check` to see all errors with file locations.
   - **ESLint errors**: Run `npm run lint` to check code quality.

Keep the README concise but complete. Target 100-150 lines. No emojis. Use plain markdown formatting.
  </action>
  <verify>
    - README.md exists with all 8 sections
    - Quick Start has 4 steps (navigate, install, start, open browser)
    - All 5 npm scripts documented
    - Project structure matches actual `src/` directory layout
    - Tech stack versions match package.json
  </verify>
  <done>
    - Default Vite template README replaced with comprehensive project documentation
    - Quick Start enables setup in under 5 minutes
    - All npm scripts documented including type-check
    - Project structure, key patterns, and troubleshooting documented
    - QUAL-UI-05 (README documentation) satisfied
  </done>
</task>

</tasks>

<verification>
1. Read README.md and verify all 8 sections present
2. Follow Quick Start steps mentally: cd frontend, npm install, npm run dev, open browser -- all commands correct
3. Verify npm script names match what is in package.json (including type-check from Plan 01)
4. Verify project structure tree matches actual src/ directory layout
5. Verify tech stack versions match package.json dependencies
</verification>

<success_criteria>
- QUAL-UI-05: Developer can read README and set up frontend in under 5 minutes
- README replaces default Vite template with project-specific documentation
- All available npm scripts are documented
- Project structure and key patterns are explained
</success_criteria>

<output>
After completion, create `.planning/phases/17-quality-&-polish/17-03-SUMMARY.md`
</output>
