---
phase: 12-frontend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/vite-env.d.ts
  - frontend/index.html
autonomous: true

must_haves:
  truths:
    - "Developer runs `cd frontend && npm install && npm run dev` and sees React app at localhost:5173"
    - "Tailwind CSS utility classes render styled elements correctly in the browser"
    - "Path alias @/ resolves correctly in imports (both IDE and Vite build)"
    - "TypeScript compiles without errors via `npm run build`"
  artifacts:
    - path: "frontend/package.json"
      provides: "Project dependencies and scripts"
      contains: "react"
    - path: "frontend/vite.config.ts"
      provides: "Vite configuration with Tailwind plugin and path aliases"
      contains: "@tailwindcss/vite"
    - path: "frontend/src/index.css"
      provides: "Global styles with Tailwind import"
      contains: "@import \"tailwindcss\""
    - path: "frontend/src/App.tsx"
      provides: "Root component with Tailwind-styled content"
      min_lines: 10
  key_links:
    - from: "frontend/vite.config.ts"
      to: "@tailwindcss/vite"
      via: "Vite plugin registration"
      pattern: "tailwindcss\\(\\)"
    - from: "frontend/tsconfig.json"
      to: "frontend/tsconfig.app.json"
      via: "TypeScript project references"
      pattern: "references"
---

<objective>
Scaffold a React + Vite + TypeScript application with Tailwind CSS v4 in the `frontend/` directory.

Purpose: Establish the foundational frontend project that all subsequent phases build upon. This creates the dev server, build pipeline, and CSS framework.
Output: A working React app at localhost:5173 with Tailwind CSS utility classes rendering correctly.
</objective>

<execution_context>
@/Users/craigcronin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/craigcronin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-frontend-foundation/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite React-TS project and install Tailwind CSS v4</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/tsconfig.node.json
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/index.css
    frontend/src/vite-env.d.ts
    frontend/index.html
  </files>
  <action>
    1. From the project root, scaffold the Vite project:
       ```bash
       npm create vite@latest frontend -- --template react-ts
       cd frontend
       npm install
       ```

    2. Install Tailwind CSS v4 with Vite plugin:
       ```bash
       npm install tailwindcss @tailwindcss/vite
       ```

    3. Install @types/node for path.resolve in vite.config.ts:
       ```bash
       npm install -D @types/node
       ```

    4. Update `vite.config.ts` to add Tailwind plugin and path alias:
       ```typescript
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'
       import tailwindcss from '@tailwindcss/vite'
       import path from 'path'

       export default defineConfig({
         plugins: [react(), tailwindcss()],
         resolve: {
           alias: {
             '@': path.resolve(__dirname, './src'),
           },
         },
       })
       ```

    5. Configure path aliases in `tsconfig.app.json` — add to compilerOptions:
       ```json
       "baseUrl": ".",
       "paths": {
         "@/*": ["./src/*"]
       }
       ```

    6. Replace `src/index.css` with Tailwind import:
       ```css
       @import "tailwindcss";
       ```

    7. Replace `src/App.tsx` with a minimal component using Tailwind classes:
       ```tsx
       function App() {
         return (
           <div className="min-h-screen bg-gray-50 flex items-center justify-center">
             <div className="text-center">
               <h1 className="text-3xl font-bold text-gray-900">
                 MTI WiFi Monitor
               </h1>
               <p className="mt-2 text-gray-600">
                 Factory & Gateway Management
               </p>
             </div>
           </div>
         )
       }

       export default App
       ```

    8. Clean up default Vite files: Remove `src/App.css` if it exists (styles are in index.css via Tailwind). Remove any Vite logo SVG files in `src/` or `public/` that were scaffolded.
  </action>
  <verify>
    Run from the frontend directory:
    ```bash
    cd frontend && npm run build
    ```
    Build completes with zero TypeScript errors.

    Then verify Tailwind is integrated:
    ```bash
    grep -q '@import "tailwindcss"' frontend/src/index.css && echo "PASS: Tailwind import found"
    grep -q 'tailwindcss()' frontend/vite.config.ts && echo "PASS: Tailwind plugin configured"
    grep -q '"@/*"' frontend/tsconfig.app.json && echo "PASS: Path alias configured"
    ```
  </verify>
  <done>
    - `npm run build` in frontend/ completes with zero errors
    - vite.config.ts includes tailwindcss() plugin and @/ path alias
    - tsconfig.app.json has @/* path mapping
    - index.css imports Tailwind
    - App.tsx uses Tailwind utility classes
    - No leftover default Vite boilerplate (App.css, logos)
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` exits with code 0
2. `grep '@import "tailwindcss"' frontend/src/index.css` returns a match
3. `grep 'tailwindcss()' frontend/vite.config.ts` returns a match
4. `grep '"@/\*"' frontend/tsconfig.app.json` returns a match
5. `ls frontend/src/App.css 2>/dev/null` returns nothing (cleaned up)
</verification>

<success_criteria>
- SETUP-01 satisfied: React + Vite application builds and runs with TypeScript
- SETUP-02 satisfied: Tailwind CSS configured and working (v4 with Vite plugin)
- frontend/ directory exists with complete Vite + React + TS + Tailwind project
- Build succeeds, path aliases resolve, Tailwind classes ready to use
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-foundation/12-01-SUMMARY.md`
</output>
