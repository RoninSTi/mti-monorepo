---
phase: 13-component-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/table.tsx
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/label.tsx
  - frontend/src/components/ui/badge.tsx
autonomous: true

must_haves:
  truths:
    - "All shadcn UI components (Input, Card, Table, Dialog, Label, Badge) can be imported from @/components/ui/*"
    - "react-router-dom, zod, and @hookform/resolvers are installed and available"
    - "TypeScript compilation succeeds with zero errors after adding new components"
    - "Each component has full TypeScript prop types and supports className override via cn()"
  artifacts:
    - path: "frontend/src/components/ui/input.tsx"
      provides: "Input component with forwardRef and className support"
    - path: "frontend/src/components/ui/card.tsx"
      provides: "Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter components"
    - path: "frontend/src/components/ui/table.tsx"
      provides: "Table, TableHeader, TableBody, TableRow, TableHead, TableCell components"
    - path: "frontend/src/components/ui/dialog.tsx"
      provides: "Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter components"
    - path: "frontend/src/components/ui/label.tsx"
      provides: "Label component for form fields"
    - path: "frontend/src/components/ui/badge.tsx"
      provides: "Badge component with variant support"
  key_links:
    - from: "frontend/src/components/ui/*.tsx"
      to: "@/lib/utils"
      via: "cn() import for class merging"
      pattern: "import.*cn.*from.*@/lib/utils"
---

<objective>
Install all Phase 13 dependencies and add shadcn/ui component library primitives.

Purpose: Establish the UI component foundation (COMP-01, COMP-03, COMP-04, COMP-05) that routing/layout and form plans depend on. All shadcn components are added via CLI and committed as project-owned source code.

Output: Six new shadcn/ui components in `frontend/src/components/ui/`, three npm packages installed (react-router-dom, zod, @hookform/resolvers).
</objective>

<execution_context>
@/Users/craigcronin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/craigcronin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-component-architecture/13-RESEARCH.md
@frontend/components.json
@frontend/package.json
@frontend/src/components/ui/button.tsx
@frontend/src/lib/utils.ts
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm dependencies for routing, validation, and form resolution</name>
  <files>frontend/package.json</files>
  <action>
    Run from the `frontend/` directory:

    ```bash
    npm install react-router-dom zod @hookform/resolvers
    ```

    This installs:
    - `react-router-dom` — React Router v6 for client-side routing (NAV-03)
    - `zod` — Schema validation library for form validation (COMP-02 prep)
    - `@hookform/resolvers` — Connects Zod schemas to React Hook Form (COMP-02 prep)

    These packages are needed by Plans 02 and 03. Installing here so both parallel plans have dependencies available.

    After installation, verify `package.json` has all three new dependencies listed.
  </action>
  <verify>
    Run `cd frontend && npm ls react-router-dom zod @hookform/resolvers` — all three listed without errors.
    Run `cd frontend && npx tsc --noEmit` — zero TypeScript errors.
  </verify>
  <done>
    react-router-dom, zod, and @hookform/resolvers appear in frontend/package.json dependencies. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add shadcn/ui components (Input, Card, Table, Dialog, Label, Badge)</name>
  <files>
    frontend/src/components/ui/input.tsx
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/table.tsx
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/label.tsx
    frontend/src/components/ui/badge.tsx
  </files>
  <action>
    Run from the `frontend/` directory, adding each component via the shadcn CLI:

    ```bash
    npx shadcn@latest add input --yes
    npx shadcn@latest add card --yes
    npx shadcn@latest add table --yes
    npx shadcn@latest add dialog --yes
    npx shadcn@latest add label --yes
    npx shadcn@latest add badge --yes
    ```

    The `--yes` flag accepts defaults without prompting. The shadcn CLI reads `components.json` (already configured in Phase 12) and generates components into `frontend/src/components/ui/`.

    After generation, verify each file:
    - Exists and exports named components
    - Uses `cn()` from `@/lib/utils` for class merging
    - Has proper TypeScript types (forwardRef, VariantProps where applicable)
    - Uses CSS theme variables (bg-primary, text-foreground, etc.) not hardcoded colors

    NOTE: shadcn CLI may install additional Radix UI dependencies automatically (e.g., @radix-ui/react-dialog, @radix-ui/react-label). This is expected behavior.

    Component inventory after this task:
    - Button (exists from Phase 12)
    - Input (new — text inputs for forms)
    - Card (new — content containers for pages)
    - Table (new — data display for factory/gateway lists)
    - Dialog (new — modal for confirmations and forms)
    - Label (new — form field labels with accessibility)
    - Badge (new — status indicators)
  </action>
  <verify>
    Run `cd frontend && npx tsc --noEmit` — zero TypeScript errors.
    Run `ls frontend/src/components/ui/` — should show button.tsx, input.tsx, card.tsx, table.tsx, dialog.tsx, label.tsx, badge.tsx.
    Verify each component file imports `cn` from `@/lib/utils`.
  </verify>
  <done>
    Seven UI components exist in frontend/src/components/ui/ (Button + 6 new). All import cn() utility, have TypeScript types, use CSS variables for theming, and compile without errors. This satisfies COMP-01 (shared UI component library), COMP-04 (TypeScript prop types), and COMP-05 (consistent Tailwind patterns).
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` passes with zero errors
2. `ls frontend/src/components/ui/` lists 7 component files (button, input, card, table, dialog, label, badge)
3. `cd frontend && npm ls react-router-dom zod @hookform/resolvers` shows all packages installed
4. Each component file uses `cn()` from `@/lib/utils` and CSS theme variables
</verification>

<success_criteria>
- All 7 shadcn/ui components exist and compile
- react-router-dom, zod, @hookform/resolvers installed
- Zero TypeScript errors
- Components follow consistent pattern: forwardRef, cn() class merging, CSS variables
- COMP-01 (shared UI library), COMP-04 (TypeScript types), COMP-05 (consistent styling) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/13-component-architecture/13-01-SUMMARY.md`
</output>
